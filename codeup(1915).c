/*
#include <stdio.h>
#pragma warning(disable : 4996)


int f(int n)
{
	if (n <= 2)
		return 1;
	else 
		return f(n-1)+f(n-2);
	
}

int main(void)
{
	int num = 0;
	scanf("%d",&num);

	printf("%d", f(num));

	return 0;
}
*/